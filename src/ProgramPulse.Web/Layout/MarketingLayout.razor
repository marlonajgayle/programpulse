@inherits LayoutComponentBase

<div class="marketing-shell">
    @Body
</div>

@code {
    // Marketing pages render full-bleed with no app chrome.
    // Header and footer live in the page so the layout stays a thin wrapper.
}
